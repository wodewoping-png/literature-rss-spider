# 文献 RSS 自动采集与周报系统

本项目从学术期刊 RSS 源抓取新能源、电池、碳中和与 CCUS 等领域的最新文献，补全
摘要和作者信息，按周聚合，再生成分类后的中英双语 Excel/Word 周报和月度统计表。

## 生产主链路

```text
feeds1211.txt
  -> spider0301.py                         # 日抓取与摘要补全
  -> output/news_with_abstract_*.csv
  -> weekly_aggrerate_with_abs.py         # 滚动 7 个发表日聚合、去重
  -> output/weekly/weekly_news_with_abstract_*.csv
  -> classify_weekly_onefile.py           # 规则/向量召回 + Gemini 最终分类
  -> translated CSV / XLSX / DOCX         # Google Translate 可选
  -> monthly_literature_stats.py          # 月度统计
```

正式分类继续使用 Google AI Studio / Gemini。翻译是独立可选步骤，支持 Google Translate
或跳过。分类类别和包括/排除规则统一维护在 `classification.txt`。

## 快速验证

```bash
GEMINI_NETWORK_DISABLED=1 python -m unittest discover -v
python -m compileall -q -x '(output|filtered_news|csv|.git)' .
```

所有 Gemini 分类测试均使用 Mock；`GEMINI_NETWORK_DISABLED=1` 会在建立 HTTP 请求前直接
阻止调用，因此本地和 CI 测试不要求模型密钥，也不会产生 Gemini API 请求。

## 依赖分组

- `requirements-spider.txt`：RSS 抓取、网页解析和 Playwright。
- `requirements-report.txt`：CSV、Excel、Word、翻译与月度统计。
- `requirements-classifier.txt`：报告依赖、Torch、Transformers 和句向量模型。
- `requirements-analytics.txt`：历史/专项分析和绘图。
- `requirements.txt`：聚合上述依赖，供完整本地环境使用。

详细职责与测试边界见 `ARCHITECTURE.md` 和 `项目流程说明.md`。
