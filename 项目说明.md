# 文献 RSS 自动采集与周报系统

本项目从学术期刊 RSS 源抓取新能源、电池、碳中和与 CCUS 等领域的最新文献，补全
摘要和作者信息，按周聚合，再生成分类后的中英双语 Excel/Word 周报和月度统计表。

## 生产主链路

```text
feeds1211.txt
  -> spider0301.py                         # 日抓取与摘要补全
  -> output/news_with_abstract_*.csv
  -> weekly_aggrerate_with_abs.py         # 滚动 7 个发表日聚合、去重
  -> output/weekly/weekly_news_with_abstract_*.csv
  -> classify_weekly_onefile.py prepare   # 生成 Codex 输入和 JSON Schema
  -> Codex                                # 按 classification.txt 分类
  -> classify_weekly_onefile.py export    # 校验、翻译、导出 XLSX/DOCX
  -> monthly_literature_stats.py          # 月度统计
```

分类只使用 Codex，不调用 Gemini API。翻译是独立可选步骤，目前支持 Google Translate
或跳过翻译。

## 快速验证

```bash
python -m unittest discover -v
python -m compileall -q -x '(output|filtered_news|csv|.git)' .
```

所有分类单元测试均为离线文件契约测试，不要求模型密钥，也不发起模型网络请求。

## 依赖分组

- `requirements-spider.txt`：RSS 抓取、网页解析和 Playwright。
- `requirements-report.txt`：CSV、Excel、Word、翻译与月度统计。
- `requirements-analytics.txt`：历史/专项语义分析脚本的大模型本地依赖。
- `requirements.txt`：聚合上述依赖，供需要完整本地环境时使用。

详细职责和 GitHub Actions 权限边界见 `ARCHITECTURE.md` 与 `项目流程说明.md`。
