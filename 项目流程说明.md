# 生产流程与运维说明

## 1. 日抓取与周聚合

`.github/workflows/rss_abs.yaml` 每天运行 `spider0301.py`，输出含摘要的日 CSV；每周五
在日报完成后运行 `weekly_aggrerate_with_abs.py`，合并滚动 7 个发表日的数据并按 DOI、
规范化链接或标题去重。

该 workflow 只安装 `requirements-spider.txt`。旧版并行 RSS workflow 已移除，避免重复
抓取、重复提交和多个任务同时向 `main` push。

## 2. Gemini 分类与周报导出

`.github/workflows/csv_to_xlsx.yaml` 每周寻找最新未处理的周 CSV，安装
`requirements-classifier.txt`，然后运行 `classify_weekly_onefile.py`：

1. 解析 `classification.txt` 的类别定义、包括项和排除项。
2. 用排除规则阻断误分类、用强关键词直接分类。
3. 使用 sentence-transformers 召回候选类别。
4. 使用 Gemini 对剩余条目进行最终多标签确认；按稳定 ID 批量返回，漏项重试一次。
5. 可选使用 Google Translate 翻译标题/摘要。
6. 使用断点文件保存翻译与分类进度，最终导出 CSV、XLSX 和 DOCX。

仓库生产 workflow 需要配置 `GOOGLE_AI_API_KEY`（或代码支持的 `GEMINI_API_KEY`）。

## 3. 月度统计

`.github/workflows/monthly_literature_stats.yaml` 在 Gemini 周报 workflow 成功后触发，并有
每周五兜底调度。只有当前周五是该月最后一个周五时才生成当月统计；手动触发可指定
`YYYY-MM`。

## 4. 本地离线测试

测试只验证程序逻辑，不访问 Gemini：

```bash
GEMINI_NETWORK_DISABLED=1 \
GOOGLE_AI_API_KEY= GEMINI_API_KEY= \
python -m unittest discover -v
```

测试使用固定 JSON Mock Gemini 返回，并用假的句向量模型执行混合分类；仓库内最新真实周
CSV 也会在同一 Mock 路径下全量遍历。`gemini_client.py` 的网络保险保证即使机器里残留了
真实密钥，也无法在该测试进程中发出 Gemini 请求。

如需人工验证非模型导出路径，可使用 `--skip-translate --skip-gpt`；完整生产分类则不要
设置 `GEMINI_NETWORK_DISABLED`，并提供 Gemini 密钥。

## 5. 维护原则

- 新逻辑进入当前主脚本，避免复制带日期的新版本脚本。
- 分类规则只维护 `classification.txt`。
- 生产 workflow 安装最小依赖集。
- 测试文件必须是可由 `unittest discover` 执行的离线测试，不能伪装成测试脚本调用模型。
- `output/debug/` 和本地备份文件不提交到 Git。
