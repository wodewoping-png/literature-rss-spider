# 生产流程与运维说明

## 1. 日抓取与周聚合

`.github/workflows/rss_abs.yaml` 每天运行 `spider0301.py`，输出含摘要的日 CSV；每周五
在日报完成后运行 `weekly_aggrerate_with_abs.py`，合并滚动 7 个发表日的数据并按 DOI、
规范化链接或标题去重。

该 workflow 只安装 `requirements-spider.txt`。旧版并行 RSS workflow 已移除，避免重复
抓取、重复提交和多个 job 同时向 `main` push。

## 2. Codex 分类与周报导出

`.github/workflows/csv_to_xlsx.yaml` 分成三个隔离 job：

1. `prepare`：寻找最新未处理周 CSV，生成 `output/codex/classification-input.json` 和
   `classification-schema.json`。
2. `classify`：将输入按约 150 条分批，最多并行 3 批使用
   `openai/codex-action@v1`，按 `classification.txt` 返回结构化 JSON。
   此 job 的仓库权限为只读，checkout 不保留凭据，不运行仓库代码。
3. `export`：合并所有批次，严格检查全部 id 是否完整、类别是否有效，再执行可选翻译并导出
   CSV/XLSX/DOCX。该 job 可以写仓库，但拿不到 OpenAI 密钥。

分类不会调用 Gemini API。GitHub 仓库需要配置 `OPENAI_API_KEY` secret 供 Codex Action
使用；不要把密钥设置成整个 job 的环境变量。

## 3. 月度统计

`.github/workflows/monthly_literature_stats.yaml` 在 Codex 周报 workflow 成功后触发，并有
每周五的兜底调度。只有当当前周五是该月最后一个周五时才生成当月统计；手动触发可显式
指定 `YYYY-MM`。

## 4. 本地 dry run

只生成 Codex 输入（无网络）：

```bash
python classify_weekly_onefile.py prepare \
  --input output/weekly/weekly_news_with_abstract_YYYY-MM-DD.csv
```

获得符合 schema 的 Codex JSON 后导出（可用 `--translation-provider none` 完全离线）：

```bash
python classify_weekly_onefile.py export \
  --input output/weekly/weekly_news_with_abstract_YYYY-MM-DD.csv \
  --codex-result output/codex/classification-result.json \
  --translation-provider none
```

`export` 遇到缺失 id、重复 id、未知 id 或未知类别会直接失败，不生成看似完整但实际漏项
的周报。

## 5. 维护原则

- 新逻辑进入当前主脚本，避免复制带日期的新版本脚本。
- 分类规则只维护 `classification.txt`，Codex 输入与 schema 由代码生成。
- 生产 workflow 安装最小依赖集；专项分析显式安装 `requirements-analytics.txt`。
- `output/codex/` 是临时交换目录，不提交到 Git。
