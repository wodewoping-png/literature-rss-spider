# 代码与文档文件职责

## 生产代码

| 文件 | 职责 |
|---|---|
| `spider0301.py` | RSS 抓取、日期过滤、摘要/末位作者多级补全和日 CSV 导出 |
| `weekly_aggrerate_with_abs.py` | 滚动 7 个发表日聚合、字段合并和去重 |
| `classification_io.py` | Codex 输入、JSON Schema、结果完整性与类别白名单校验 |
| `classify_weekly_onefile.py` | prepare/export CLI、可选翻译、Excel/Word 周报导出 |
| `excel_output_utils.py` | 日期、DOI 和链接的统一 Excel 格式处理 |
| `monthly_literature_stats.py` | 按月读取周报并填充月度统计模板 |

## 配置和依赖

| 文件 | 职责 |
|---|---|
| `feeds1211.txt` | 当前 RSS 源清单 |
| `classification.txt` | Codex 使用的类别名称、包括项与排除项 |
| `requirements-spider.txt` | 抓取链路依赖 |
| `requirements-report.txt` | 周报和月报依赖 |
| `requirements-analytics.txt` | 历史/专项分析依赖 |
| `requirements.txt` | 完整本地环境的聚合入口 |

## GitHub Actions

| 文件 | 职责 |
|---|---|
| `.github/workflows/rss_abs.yaml` | 日抓取和周聚合 |
| `.github/workflows/csv_to_xlsx.yaml` | Codex 分类与周报导出 |
| `.github/workflows/monthly_literature_stats.yaml` | 月度统计 |

## 测试

| 文件 | 覆盖范围 |
|---|---|
| `test_classification_io.py` | 分类规则、payload/schema、Codex 结果严格校验 |
| `test_classify_weekly_onefile.py` | 离线 prepare/export、稳定 id、XLSX/DOCX 输出 |
| `test_spider_abstract_sources.py` | RSS/网页摘要来源解析 |
| `test_weekly_aggrerate_with_abs.py` | 7 个发表日窗口 |
| `test_excel_output_utils.py` | Excel 日期、DOI 与超链接格式（相关分支合入后生效） |

## 历史与专项代码

带旧日期的爬虫、`best_*`、`filter_*`、`filtered_news/` 和 `csv/` 下的脚本不在默认
GitHub Actions 中运行，仅用于历史追溯或专项分析。它们如需继续维护，应逐步抽取共享模块，
不要重新接入生产 workflow。

旧 Gemini 周报脚本和伪装成单元测试的联网分类脚本已删除；完整版本仍可从 Git 历史和本次
全量备份恢复。
